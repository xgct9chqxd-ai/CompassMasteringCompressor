#include <iostream>
#include "reference_core/reference_core.h"

int main()
{
    const bool ok = (reference_core::kReferenceCoreId != nullptr);
    if (! ok)
    {
        std::cout << "reference_tests FAIL\n";
        return 1;
    }

    std::cout << "reference_tests PASS\n";
    return 0;
}
